# Visual System Diagrams

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        ATTENDANCE BRIDGE SYSTEM                      │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  ZKTeco Device 1 │     │  ZKTeco Device 2 │     │  ZKTeco Device N │
│   192.168.1.100  │     │   192.168.1.101  │     │   192.168.1.10N  │
│    Port: 4370    │     │    Port: 4370    │     │    Port: 4370    │
└────────┬─────────┘     └────────┬─────────┘     └────────┬─────────┘
         │                        │                        │
         │        TCP/IP Connection (pyzk library)         │
         │                        │                        │
         └────────────────────────┴────────────────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │   CELERY BEAT SCHEDULER   │
                    │   (Every 15 minutes)      │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │    CELERY WORKER TASK     │
                    │    poll_devices_task()    │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │    DJANGO APPLICATION     │
                    │   device_utils.py         │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │     RAW ATTENDANCE DB     │
                    │  (All device punches)     │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │   CELERY WORKER TASK      │
                    │ process_attendance_task() │
                    │   (Hourly)                │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │   processing_utils.py     │
                    │ - Group by user & date    │
                    │ - Extract clock-in/out    │
                    │ - Detect outliers         │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │  PROCESSED ATTENDANCE DB  │
                    │  (Daily clock-in/out)     │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │   CELERY WORKER TASK      │
                    │   sync_to_crm_task()      │
                    │   (Every 30 minutes)      │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │     crm_utils.py          │
                    │  HTTPS POST with Token    │
                    │  Retry on failure         │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │   REMOTE CRM SYSTEM       │
                    │   https://your-crm.com    │
                    └───────────────────────────┘
```

## Data Flow Timeline

```
TIME         ACTION                  COMPONENT               DATA
────────────────────────────────────────────────────────────────────────
00:00        Device Polling          Celery Beat             -
00:00:05     Connect to Device 1     device_utils.py         TCP Connection
00:00:10     Fetch Attendance        pyzk library            Raw logs
00:00:15     Store in DB             RawAttendance model     120 punches
00:00:20     Update last_sync        Device model            timestamp
00:00:25     Connect to Device 2     device_utils.py         TCP Connection
...          (repeat for all)        ...                     ...

01:05        Process Attendance      Celery Beat             -
01:05:05     Query Raw Data          processing_utils.py     All unprocessed
01:05:10     Group by User/Date      Python logic            User groups
01:05:15     Extract Clock-in        First punch             08:15:30
01:05:20     Extract Clock-out       Last punch              17:30:00
01:05:25     Check Outliers          is_outlier()            False
01:05:30     Save Processed          ProcessedAttendance     50 records

01:30        CRM Sync                Celery Beat             -
01:30:05     Query Unsynced          ProcessedAttendance     20 records
01:30:10     Prepare JSON            prepare_attendance()    JSON payload
01:30:15     POST to CRM             requests library        HTTPS
01:30:20     Receive Response        CRM Server              200 OK
01:30:25     Mark as Synced          ProcessedAttendance     synced=True

06:00        Retry Failed Syncs      Celery Beat             -
06:00:05     Query Failed            sync_attempts >= 1      5 records
06:00:10     Retry Each              send_to_crm()           HTTPS POST
...

02:00        Daily Cleanup           Celery Beat             -
02:00:05     Delete Old Raw          > 90 days old           500 records
02:00:10     Deduplicate             Check duplicates        0 found
```

## Component Interaction

```
┌────────────────────────────────────────────────────────────────┐
│                      USER INTERACTIONS                          │
└────────────────────────────────────────────────────────────────┘

┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ Django Admin │    │  REST API    │    │   CLI Tools  │
│ http://..    │    │ /api/...     │    │ manage.py    │
└──────┬───────┘    └──────┬───────┘    └──────┬───────┘
       │                   │                   │
       └───────────────────┴───────────────────┘
                           │
       ┌───────────────────▼───────────────────┐
       │         DJANGO APPLICATION            │
       │  ┌─────────────────────────────────┐  │
       │  │  Models (ORM)                   │  │
       │  │  - Device                       │  │
       │  │  - RawAttendance                │  │
       │  │  - ProcessedAttendance          │  │
       │  └─────────────┬───────────────────┘  │
       │                │                       │
       │  ┌─────────────▼───────────────────┐  │
       │  │  Business Logic                 │  │
       │  │  - device_utils.py              │  │
       │  │  - processing_utils.py          │  │
       │  │  - crm_utils.py                 │  │
       │  └─────────────┬───────────────────┘  │
       │                │                       │
       │  ┌─────────────▼───────────────────┐  │
       │  │  Views & Serializers            │  │
       │  │  - API endpoints                │  │
       │  │  - Admin interface              │  │
       │  └─────────────────────────────────┘  │
       └───────────────────────────────────────┘
                           │
       ┌───────────────────┴───────────────────┐
       │                                       │
┌──────▼────────┐                   ┌──────────▼─────────┐
│   Database    │                   │  Celery + Redis    │
│  (SQLite)     │                   │  (Background Jobs) │
│               │                   │                    │
│ - Devices     │                   │ - Device Polling   │
│ - Raw Logs    │                   │ - Processing       │
│ - Processed   │                   │ - CRM Sync         │
└───────────────┘                   │ - Cleanup          │
                                    └────────────────────┘
```

## Background Task Scheduler

```
CELERY BEAT SCHEDULE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌──────────────────────────────────────────────────────┐
│  Task: poll_devices                                  │
│  Schedule: Every 15 minutes                          │
│  Function: core.tasks.poll_devices_task()            │
│  Action: Fetch attendance from all devices           │
└──────────────────────────────────────────────────────┘
    ↓ Stores in RawAttendance

┌──────────────────────────────────────────────────────┐
│  Task: process_attendance                            │
│  Schedule: Every hour (at :05 past)                  │
│  Function: core.tasks.process_attendance_task()      │
│  Action: Process raw logs into clock-in/out          │
└──────────────────────────────────────────────────────┘
    ↓ Stores in ProcessedAttendance

┌──────────────────────────────────────────────────────┐
│  Task: sync_to_crm                                   │
│  Schedule: Every 30 minutes                          │
│  Function: core.tasks.sync_to_crm_task()             │
│  Action: Send processed records to CRM               │
└──────────────────────────────────────────────────────┘
    ↓ Marks records as synced

┌──────────────────────────────────────────────────────┐
│  Task: retry_failed_syncs                            │
│  Schedule: Every 6 hours (at :30 past)               │
│  Function: core.tasks.retry_failed_syncs_task()      │
│  Action: Retry previously failed CRM syncs           │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│  Task: daily_cleanup                                 │
│  Schedule: Daily at 2:00 AM                          │
│  Function: core.tasks.daily_cleanup_task()           │
│  Action: Delete old records, deduplicate             │
└──────────────────────────────────────────────────────┘
```

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────┐
│              DEVICE POLLING ERROR HANDLING              │
└─────────────────────────────────────────────────────────┘

Device Connection
        │
        ├─ Success ──────────────────┐
        │                            │
        └─ Failure ──> Log Error ────┤
           │                         │
           ├─ Timeout               ▼
           ├─ Connection Refused    Continue to
           ├─ Network Error         Next Device
           └─ Device Offline        


┌─────────────────────────────────────────────────────────┐
│              CRM SYNC ERROR HANDLING                    │
└─────────────────────────────────────────────────────────┘

Send to CRM
        │
        ├─ Success (200/201) ──> Mark Synced
        │
        └─ Failure ──> Increment sync_attempts
           │
           ├─ Timeout ──────────────┐
           ├─ 4xx Client Error ─────┤
           ├─ 5xx Server Error ─────┤ Log Error
           └─ Network Error ────────┘      │
                  │                        │
                  └──> Retry Later (Celery Beat)
                       │
                       └──> Max 3 Attempts
                            │
                            ├─ < 3 ──> Retry
                            └─ >= 3 ─> Mark as Failed
```

## Outlier Detection Logic

```
┌─────────────────────────────────────────────────────────┐
│              OUTLIER DETECTION FLOWCHART                │
└─────────────────────────────────────────────────────────┘

Configuration:
  WORK_START_TIME = 08:00
  WORK_END_TIME = 18:00

For Each Processed Attendance Record:

Clock-in Time Check:
  ┌────────────────────────────────────────┐
  │ Clock-in < 06:00 (2h before start)?   │
  │ YES ──> FLAG: "Clock-in too early"    │
  └────────────────────────────────────────┘
  ┌────────────────────────────────────────┐
  │ Clock-in > 10:00 (2h after start)?    │
  │ YES ──> FLAG: "Clock-in too late"     │
  └────────────────────────────────────────┘

Clock-out Time Check:
  ┌────────────────────────────────────────┐
  │ Clock-out < 16:00 (2h before end)?    │
  │ YES ──> FLAG: "Clock-out too early"   │
  └────────────────────────────────────────┘
  ┌────────────────────────────────────────┐
  │ Clock-out > 21:00 (3h after end)?     │
  │ YES ──> FLAG: "Clock-out too late"    │
  └────────────────────────────────────────┘

Result:
  Any flags? ──> is_outlier = True
              ──> outlier_reason = "..."
  No flags?  ──> is_outlier = False
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────┐
│                   PRODUCTION DEPLOYMENT                   │
└──────────────────────────────────────────────────────────┘

                    Internet
                       │
                       ▼
              ┌────────────────┐
              │  Nginx (443)   │  SSL/TLS Termination
              │  Reverse Proxy │
              └────────┬───────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
┌───────────────┐            ┌───────────────┐
│  Gunicorn     │            │  Static Files │
│  (port 8000)  │            │  /static/     │
└───────┬───────┘            └───────────────┘
        │
        ▼
┌───────────────────────────────────────────┐
│        Django Application                 │
│  ┌─────────────────────────────────────┐  │
│  │  attendance_bridge/                 │  │
│  │  core/                              │  │
│  └─────────────────────────────────────┘  │
└───────┬───────────────────────────────────┘
        │
┌───────┴────────┬─────────────┬────────────┐
│                │             │            │
▼                ▼             ▼            ▼
PostgreSQL    Redis       Celery       Celery
Database      Broker      Worker       Beat
(5432)        (6379)      Process      Scheduler
```

## File Organization

```
zkteco-f18-attendance-bridge/
│
├── 📄 Configuration Files
│   ├── .env                    ← Your secrets (DO NOT COMMIT)
│   ├── .env.example            ← Template
│   ├── requirements.txt        ← Dependencies
│   └── .gitignore             ← Git rules
│
├── 📚 Documentation
│   ├── README.md              ← Main docs
│   ├── SETUP.md               ← Setup guide
│   ├── API_DOCS.md            ← API reference
│   ├── PROJECT_STRUCTURE.md   ← Code organization
│   ├── QUICKREF.md            ← Quick reference
│   ├── SUMMARY.md             ← Project summary
│   └── DIAGRAMS.md            ← This file
│
├── 🚀 Scripts
│   ├── setup.sh               ← Initial setup
│   ├── start_server.sh        ← Start Django
│   ├── start_worker.sh        ← Start Celery worker
│   └── start_beat.sh          ← Start Celery beat
│
├── 🏗️ Django Project
│   └── attendance_bridge/
│       ├── settings.py        ← Configuration
│       ├── celery.py          ← Celery setup
│       └── urls.py            ← Routing
│
├── 💼 Core Application
│   └── core/
│       ├── models.py          ← Database models
│       ├── device_utils.py    ← Device functions
│       ├── processing_utils.py← Processing logic
│       ├── crm_utils.py       ← CRM sync
│       ├── tasks.py           ← Celery tasks
│       ├── views.py           ← API views
│       └── management/        ← CLI commands
│
└── 📊 Runtime Files
    ├── db.sqlite3             ← Database (created)
    └── logs/                  ← Log files (created)
```
